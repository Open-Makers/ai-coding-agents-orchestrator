# Example Task

Build a minimal CLI orchestrator in Go that runs PLAN → CODE → TEST → REVIEW → FIX → DONE.

Constraints:
- No breaking changes.
- Prefer standard library.

Definition of done:
- CLI runs end-to-end with artifacts in .orchestrator/
